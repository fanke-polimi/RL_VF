using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class moveto : MonoBehaviour
{
    public psmagent psm; 
    // Start is called before the first frame update
    void Start()
    {
        psm = GetComponent<psmagent>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
